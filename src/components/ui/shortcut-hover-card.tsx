import React from 'react'
import { HoverCardContent } from './hover-card'

const HoverCardContentVariant = ({ children, shortcut }) => {
  return (
    <HoverCardContent>

    </HoverCardContent>
  )
}

export default shortcut-hover-card